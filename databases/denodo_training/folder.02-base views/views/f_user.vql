CREATE OR REPLACE VIEW f_user FOLDER = '/02-base views' AS SELECT displayname, samaccountname, userprincipalname, memberof FROM FLATTEN bv_user AS v ( v.memberof);

ALTER VIEW f_user
 LAYOUT (bv_user = [20, 20, 279, 126]);

